#!/bin/sh
# Standard Gradle wrapper script (abbreviated).
# This file is intentionally a stub - GitHub Actions will run via Gradle plugin
# without needing the wrapper jar. For local builds use Android Studio which
# regenerates the wrapper automatically.
exec gradle "$@"
